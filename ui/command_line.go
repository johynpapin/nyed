package ui

func (drawer *Drawer) drawCommandLine() {
	/* if commandLine.CurrentCommand == "" {
		return nil
	}

	var x int
	for _, r := range commandLine.CurrentCommand {
		screen.Screen.SetContent(x, commandLine.Y, r, nil, tcell.StyleDefault)
		x++
	}

	screen.Screen.ShowCursor(x, commandLine.Y)

	return nil */
}
